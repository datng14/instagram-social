import styled from 'styled-components';

import NormalImg from 'components/Img';

const Img = styled(NormalImg)`
  margin: 0 auto;
  display: block;
  width: 103px;
  height: auto;
`;

export default Img;
